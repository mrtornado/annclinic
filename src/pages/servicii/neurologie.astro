---
import { getEntry } from "astro:content";
import ServiceLayout from "../../layouts/ServiceLayout.astro";
import ServiceHero from "../../components/services/ServiceHero.tsx";
import ServiceDetails from "../../components/services/ServiceDetails.tsx";

// Get the neurology service content
const service = await getEntry("services", "neurologie");

if (!service) {
  return Astro.redirect("/404");
}

// SEO optimization for neurology - targeting "neurolog București" keywords
const seoTitle = "Neurolog București - Consultații Neurologie | ANN Clinic";
const seoDescription =
  "Neurolog București cu experiență vastă. Consultații neurologie, dureri de cap, migrene, epilepsie, AVC. Programează-te la ANN Clinic Bragadiru.";
---

<ServiceLayout service={service} title={seoTitle} description={seoDescription}>
  <!-- Service Hero Section -->
  <ServiceHero service={service} client:load />

  <!-- Service Details Section -->
  <ServiceDetails service={service} client:load />
</ServiceLayout>
